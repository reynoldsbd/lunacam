table! {
    cameras (id) {
        id -> Integer,
        friendly_name -> Text,
        hostname -> Text,
        device_key -> Text,
        enabled -> Bool,
        orientation -> Integer,
    }
}
